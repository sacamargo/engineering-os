# Rivallium — Execution Layer Fixture

Machine-readable Execution Plan proof for a multi-capability booking SaaS.

This is **not** the Rivallium product. It demonstrates how Engineering OS represents:

- Intent → Capabilities → Artifacts → Tasks → Dependencies → Gates
- Role specialization without Role = Capability
- Coverage gaps without inventing Capability IDs

Validate:

```bash
python3 contracts/validate_execution.py
```

Narrative companion (Task 20): `execution-plan.md` (added separately).
