# Skillpacks (Phase 8 — Integrated Skills)

First-class **Skill** integration layer.

```text
Capability ≠ Skill ≠ Role ≠ Agent ≠ Knowledge ≠ Tool
```

- Model: `skillpacks/model.py`
- Canonical docs: `foundation/SKILL-MODEL.md`
- Knowledge-unit procedures remain under `skills/` (`eos.skill.*`)

This package must stay data-driven. The Orchestrator must not hardcode Skill lists.
