# Marketing Skills / Corey Haines

**Status:** `unavailable`

Source material for this Skill is **not present** in the Engineering OS repository.

This directory holds the integration contract/manifest only. No methodology content has been invented.

When source is added later:

1. Update `manifest.json` provenance/source/version/license
2. Set `status` to `experimental` or `active` only if content is real
3. Keep constraints: no architecture ownership, no deploy permissions, Skill ≠ Role
